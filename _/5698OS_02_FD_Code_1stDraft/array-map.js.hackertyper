var friends = ['Luci', 'Samira', 'Juliano'];

var friendsAsObj = friends.map(function(name){
   return {
      name: name
   };
});

// friendsAsObj;
// > [{name: 'Luci'}, {name: 'Samira'}, {name: 'Juliano'}]
